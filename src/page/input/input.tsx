import {Input} from 'dist';

const InputPage = () => {
    return (
        <div>
            <h1>Input</h1>
            <fieldset>
                <legend>基本</legend>
                <Input/>
            </fieldset>
        </div>
    );
};

export default InputPage;
